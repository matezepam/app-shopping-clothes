spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/db_sprint
    username: matezepam
    password: "351759"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

security:
  jwt:
    secret: "sprint-backend-secret-key-for-jwt-authentication-2026-secure"
    expiration-ms: 86400000